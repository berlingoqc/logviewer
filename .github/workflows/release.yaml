name: Release

on:
  push:
    tags:
      - '*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Go 1.21
        uses: actions/setup-go@v5
        with:
          go-version: 1.21
      - name: Get the version
        id: tag
        run: echo "tag=${GITHUB_REF#refs/*/}" >> $GITHUB_OUTPUT
      - name: Check output
        env:
          VERSION: ${{ steps.vars.outputs.tag }}
      - name: Build for all plateform
        run: ./build.sh
      - name: Docker build
        run: docker build -t ghcr.io/berlingoqc/logviewer:${{ env.VERSION }} .
      - name: Docker push
        run: docker push ghcr.io/berlingoqc/logviewer:${{ env.VERSION }}
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: ./build/*
          draft: true
